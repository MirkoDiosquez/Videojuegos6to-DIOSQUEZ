PELOTA

extends CharacterBody2d


var direccion : Vector2 = Vector2(1;1) 
var velocidadInicial = 300.0

func _physics_process(delta: float) -> void:
    var collided := move_and_collide(direction * speed * delta) 
    if (collided):
        var normal = collided.get_normal()
        print(normal)
        direction = direction.bounce(normal)
